<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="@dimen/lp_gutter"
    android:paddingEnd="@dimen/lp_gutter"
    android:paddingTop="@dimen/lp_space_5"
    android:paddingBottom="@dimen/lp_space_2">

    <TextView
        android:id="@+id/text_dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/lp_space_3"
        android:textAppearance="@style/TextAppearance.LP.Title"
        tools:text="Bench Press (Barbell)" />

    <ImageView
        android:id="@+id/image_exercise_media"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginBottom="@dimen/lp_space_1"
        android:contentDescription="@null"
        android:scaleType="fitCenter"
        android:visibility="gone"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/text_media_attribution"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/lp_space_3"
        android:textAppearance="@style/TextAppearance.LP.Body"
        android:visibility="gone"
        tools:text="© Gym Visual — https://gymvisual.com/"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/text_dialog_note"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.LP.Body"
        android:textColor="?attr/lpInk"
        android:visibility="gone"
        tools:text="Retract scapula. Slight arch. Bar to lower chest."
        tools:visibility="visible" />

</LinearLayout>
