<?xml version="1.0" encoding="utf-8"?>
<!--
  One workout plan on the Train tab. Sits directly in the RecyclerView (no outer wrapper
  card, which is what the previous version had — a card nested inside another card padded
  the list with a visible seam on all four sides for no reason).

  AIRY spacing, since Train is a browsing surface, not a working one. Delete is an icon-only
  hairline action (matching the active-workout card's vocabulary) rather than a text button,
  since "Use Plan" and "Edit" already say enough that a trash icon needs no label.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.LP.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/lp_card_gap">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/lp_card_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/text_plan_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:textAppearance="@style/TextAppearance.LP.Heading"
                tools:text="Push Day" />

            <ImageView
                android:id="@+id/image_active_plan_badge"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="@dimen/lp_space_2"
                android:contentDescription="@string/cd_active_plan"
                android:src="@drawable/ic_checkmark"
                android:visibility="gone"
                app:tint="?attr/lpAccent"
                tools:visibility="visible" />
        </LinearLayout>

        <TextView
            android:id="@+id/text_exercise_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textAppearance="@style/TextAppearance.LP.Label"
            android:textColor="?attr/lpInkTertiary"
            tools:text="6 exercises" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/lp_space_3"
            android:baselineAligned="false"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/button_use_plan"
                style="@style/Widget.LP.Button"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/lp_card_gap_dense"
                android:layout_weight="1"
                android:minHeight="44dp"
                android:text="@string/btn_use_plan" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/button_edit_plan"
                style="@style/Widget.LP.Button.Outlined"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/lp_card_gap_dense"
                android:layout_weight="1"
                android:minHeight="44dp"
                android:text="@string/button_edit" />

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/button_delete_plan"
                style="@style/Widget.LP.Card.IconAction"
                app:strokeColor="?attr/lpNegative">

                <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@string/btn_delete_plan"
                    android:padding="@dimen/lp_icon_action_padding"
                    android:src="@drawable/ic_delete"
                    app:tint="?attr/lpNegative" />
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
